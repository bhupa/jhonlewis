<?php

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateNotificationTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('notification', function (Blueprint $table) {
            $table->increments('id');
            $table->integer('sender_id')->nullable()->default(null)->unsigned();
            $table->integer('receiver_id')->nullable()->default(null)->unsigned();
            $table->integer('order_id')->nullable()->default(null)->unsigned();
            $table->string('link');
            $table->string('message');
            $table->boolean('view')->nullable()->default(0);
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('notification');
    }
}
